# Local dev — placeholder IDs work because `wrangler dev` uses local Miniflare
# storage; the real IDs aren't consulted. The `_local` suffix on names just
# helps you spot which env you're targeting in `wrangler dev` logs.
CF_CONFIG_KV_ID=local-kv-placeholder
LOG_LEVEL=debug
