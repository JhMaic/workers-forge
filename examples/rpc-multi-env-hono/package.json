{
  "name": "rpc-multi-env-hono-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "workers-forge dev --env local",
    "dev:stage": "workers-forge dev --env stage",
    "deploy:stage": "workers-forge deploy --build --env stage",
    "build": "workers-forge build --env local",
    "build:api": "workers-forge build --env local --app api-worker"
  },
  "devDependencies": {
    "workers-forge": "latest",
    "wrangler": "^4",
    "tsx": "^4",
    "hono": "^4"
  }
}
