# For deploy:stage, replace CF_CONFIG_D1_ID with the real ID from `wrangler d1 create todos-stage`.
# For dev:stage (local wrangler dev), any non-empty string works — wrangler uses a local SQLite file.
APP_ENV=stage
CF_CONFIG_D1_ID=<your-stage-d1-database-id>